"""
DocState Constants

This module defines constants used throughout the DocState library,
including the special state names for document processing.
"""

# Special states
START = "START"  # Initial state for all documents
END = "END"      # Terminal state indicating successful processing

# Default error state suffix
DEFAULT_ERROR_SUFFIX = "_error"
